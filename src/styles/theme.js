import { BREAKPONTS } from '../constants/devices';

const theme = {
  screens: {
    mobs: BREAKPONTS.MOB_START,
    mobe: BREAKPONTS.MOB_END,
    tab: BREAKPONTS.TAB,
    desk: BREAKPONTS.DESK,
  },
};

export default theme;
